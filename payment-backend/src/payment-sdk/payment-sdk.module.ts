// src/payment-sdk/payment-sdk.module.ts
/*
import { Module } from '@nestjs/common';
import { StripeProvider } from '../../../payment-sdk/src/providers/StripeProvider';
import { PayPalProvider } from '../../../payment-sdk/src/providers/PayPalProvider';
import { PaymentProviderFactory } from '../../../payment-sdk/src/factories/PaymentProviderFactory';

@Module({
  providers: [StripeProvider, PayPalProvider, PaymentProviderFactory],
  exports: [StripeProvider, PayPalProvider, PaymentProviderFactory], // Export for use in other modules
})
export class PaymentSdkModule {}
*/